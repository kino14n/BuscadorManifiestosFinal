{
  "name": "buscador-manifiestos-final",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "cd src && npm install && npm run build",
    "build":       "cd src && npm run build",
    "start":       "node server.js"
  },
  "dependencies": {
    "cors":     "^2.8.5",
    "dotenv":   "^16.0.3",
    "express":  "^4.18.2",
    "pg":       "^8.10.0"
  }
}
